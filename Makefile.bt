# libbinio Makefile for Watcom
# Copyright (c) 2003 Simon Peter <dn.tlp@gmx.net>

SUBDIRS = src

AUX = doc\*.* README AUTHORS NEWS COPYING TODO ChangeLog
DIST = src\*.cpp src\*.h src\binioh~1.in src\Makefile.bt $(AUX) INSTALL
BINARYDIST = src\*.h src\binio.lib $(AUX)

distfile = binio10s.zip
binarydistfile = binio10.zip
installfile = INSTALL.dos
